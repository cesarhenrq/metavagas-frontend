import GlobalStyle from '@styles/global';

import Router from '@routes/Router';

export default function App() {
  return (
    <div className="App">
      <GlobalStyle />
      <Router />
    </div>
  );
}
