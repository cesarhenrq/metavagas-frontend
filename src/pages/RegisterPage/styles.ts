import styled from 'styled-components';

export const RegisterPage = styled.div`
  position: relative;

  .register-form-container {
    position: absolute;
    z-index: 1;
    top: 60px;
    right: 136px;
    width: 517px;
  }
`;
